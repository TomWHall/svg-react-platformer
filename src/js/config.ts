export default {
    containerWidth: 1920,
    containerHeight: 928,

    viewportWidth: 1920,
    viewportHeight: 864,

    playerHeight: 128,
    xUnitsPerAnimationPhase: 102,

    // speedX delta per frame as input keys are held
    forceXDelta: 0.1,

    // speedY delta per frame for gravity
    forceYDelta: 0.4,

    jumpInitialForceY: -10,

    // Maximum absolute force
    maxForceX: 6,
    maxForceY: 12
};